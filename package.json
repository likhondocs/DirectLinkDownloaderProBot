{
  "name": "direct-link-downloader-pro-bot",
  "version": "1.0.0",
  "description": "A Telegram bot for downloading files from direct links",
  "main": "api/bot.js",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "telegraf": "^4.12.2",
    "axios": "^1.3.4",
    "@vercel/blob": "^0.9.2",
    "@vercel/cron": "^0.3.1"
  },
  "devDependencies": {
    "vercel": "^28.16.15"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
